# API-Trancricao-Audio

```python
pip install requests
```

### OBS: todos os pacotes instalados pelo pip, ficam no .venv da pasta raiz do projeto (isso só se estiver usando arch linux, se não provavelmente fica no global)